//AyaApp

import 'package:flutter/material.dart';

class AyaApp extends StatelessWidget {
  const AyaApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
